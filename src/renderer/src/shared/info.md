# Todo lo que no pertenece a ninguna feature ni entity. Se divide internamente en:

shared/
├── ui/        ← componentes genéricos: Button, Input, Modal
├── lib/       ← utilidades: formatDuration, formatDate
├── api/       ← cliente HTTP, instancia de axios
├── config/    ← variables de entorno, constantes
└── types/     ← tipos globales compartidos